# TIME LIBS in Java++

## 1. time.freeze.seconds(seconds);

```
@USING::STD
@USING::TIME

time.freeze.seconds(3);

quit.program.uwu(0);
```

## 2. time.freeze.millis(milliseconds);

```
@USING::STD
@USING::TIME

time.freeze.millis(500);

quit.program.uwu(0);
```

## 3. time.get.unix.now();

``` 
@USING::STD
@USING::TIME

new.variables.in.stack current_timestamp = time.get.unix.now();

print.consolewrite.puts(current_timestamp);

quit.program.uwu(0);
```
